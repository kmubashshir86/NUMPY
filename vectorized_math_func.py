import numpy as np

array = np.array([1,2,3])

#vectorized math func

print(np.sqrt(array))
#round floor ceil log sin cos exp pi e tau

#we have array of radii we have to convert it into circle area

print(np.pi * array**2)

